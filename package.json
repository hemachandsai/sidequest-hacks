{
  "name": "sidequest-hacks",
  "version": "1.0.0",
  "description": "This package contains the code to retrieve the source url of any app from sidequest store iff app is free and exists",
  "main": "exp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argparse": "^2.0.1",
    "request": "^2.88.2"
  }
}
